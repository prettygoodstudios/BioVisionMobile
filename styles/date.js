import {StyleSheet} from "react-native";

import {MAIN_FONT, DARK_COLOR, OFF_WHITE} from "./main";

const styles = StyleSheet.create({
  picker: {
    color: DARK_COLOR,
    backgroundColor: OFF_WHITE,
    fontFamily: MAIN_FONT,
    height: 100
  }
});

export default styles;
